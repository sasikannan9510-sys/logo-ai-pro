name: Daily Logo Prompt

on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * *"

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run generator
        run: |
          echo "Here are five modern Swiss-style logo design prompts..."
